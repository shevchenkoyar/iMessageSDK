namespace iMessageSDK.Chats;

/// <summary>
/// The direction in which an <see cref="IChatQuery"/> should order results.
/// </summary>
public enum SortDirection
{
    /// <summary>Ascending order (oldest/smallest first).</summary>
    Ascending,

    /// <summary>Descending order (newest/largest first).</summary>
    Descending,
}
